from ..rule import Rule
jamendo = {
    'artist': [Rule(['name'], ['artist', 'name'])],
    'release': [Rule(['name'], ['release', 'name'])],
    'recording': [Rule(['name'], ['recording', 'name'])]
    }
